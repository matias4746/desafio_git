def minutos_segundos(minutos):
    formula = minutos * 60
    return formula

def horas_minutos(horas):
    formula = horas * 60
    return formula

if __name__ == '__main__':
    minutos_segundos() 
    horas_minutos()